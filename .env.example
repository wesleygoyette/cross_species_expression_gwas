# Environment variables for production deployment
# Copy this to .env and set appropriate values

# Django settings
SECRET_KEY=your-super-secret-key-change-this-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com
DATABASE_PATH=../database/regland.sqlite

# CORS settings
CORS_ALLOWED_ORIGINS=http://localhost,http://your-domain.com,https://your-domain.com

# Optional: Database settings for PostgreSQL (if you want to switch from SQLite)
# DATABASE_URL=postgresql://user:password@localhost:5432/regland_db